{
  "name": "signbridge-gloss-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that converts SignBridge captions into sign-language gloss tokens via the Adesso AI Hub.",
  "type": "module",
  "main": "workers/gloss/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.114.1"
  }
}
